spring:
  datasource:
    url: jdbc:mysql://db:3306/docker_db?useSSL=true&verifyServerCertificate=false
    username: docker_user
    password: c0me@m3bro!
  jpa:
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
